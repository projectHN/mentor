<?php
	$this->headTitle($this->translate('Kiểm tra mã nhân sự mới ofice'));
	$paginator = $this->paginator;
	/*@var $paginator \Zend\Paginator\Paginator */
?>
<input type='hidden' id="redirectUri" value="<?= $this->redirectUri?>" />
<input type='hidden' id="defaultRedirectUri" value="<?= $this->defaultRedirectUri?>" />
Mã nhân sự office mới: <?= $this->code?><br/>
Tổng công :<?php if($this->code){echo count(explode(',', $this->code));}?><br/>
<script type="text/javascript">
	setTimeout(function(){
		if(document.getElementById("redirectUri").value != "") {
			window.location = document.getElementById("redirectUri").value;
		} else if(document.getElementById("defaultRedirectUri").value != "") {
			setTimeout(function(){
				window.location = document.getElementById("defaultRedirectUri").value;
			}, 600000);
		}
	}, 500);
</script>
<a href="<?= $this->defaultRedirectUri?>" class='lnk'>Restart [<?= $paginator->getCurrentPageNumber() .' / '. $paginator->count() ?>]</a><br><br>